import random
print("gimme words.")
choose = random.randint(1, 2)
print("as many words as you want. Once you have at least 1 word, you can type, 'done' to stop.")
add_thing = "no."
# inputs
nouns = []
verbs = []
places = []
adjectives = []

# Nouns
while "done" not in add_thing:
    add_thing = input("NOUNS:")
    list.append(nouns, add_thing)

# Verbs
while "done" not in add_thing:
    add_thing = input("VERBS:")
    list.append(verbs, add_thing)

# Places
while "done" not in add_thing:
    add_thing = input("PLACES:")
    list.append(places, add_thing)

# Adjectives
    while "done" not in add_thing:
        add_thing = input("ADJECTIVES:")
        list.append(adjectives, add_thing)

print()

print()

# Story

print()
